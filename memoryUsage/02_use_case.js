// Создаём массив длиной 6120, заполненный единицами
const arr = new Array(6120).fill(1);

// Функция для измерения потребления памяти функцией fn
const measure = (fn, label = 'Function') => {
  // Снимок памяти до выполнения
  const before = process.memoryUsage();

  // Выполнение тестируемой функции
  fn();

  // Снимок памяти после выполнения
  const after = process.memoryUsage();

  // Расчёт разницы в памяти (heapUsed) и вывод в мегабайтах
  const used = ((after.heapUsed - before.heapUsed) / 1024 / 1024).toFixed(2);
  console.log(`[${label}] Used heap: ${used} MB`);
};

// ❗ Медленная рекурсивная функция
// Использует arr.slice(1), создавая новый массив на каждом шаге
// Это приводит к большому количеству временных массивов → больше памяти
const getLengthSlice = (arr) => {
  if (arr[0] === undefined) return 0;
  return 1 + getLengthSlice(arr.slice(1));
};

// ✅ Быстрая рекурсивная функция
// Использует индекс вместо создания новых массивов
// Работает в том же рекурсивном стиле, но гораздо экономнее по памяти
const getLengthIndex = (arr, i = 0) => {
  if (arr[i] === undefined) return 0;
  return 1 + getLengthIndex(arr, i + 1);
};

// 📏 Замер памяти при выполнении "медленной" slice-рекурсии
measure(() => getLengthSlice(arr), 'Slice recursion');

// 📏 Замер памяти при выполнении "быстрой" index-рекурсии
measure(() => getLengthIndex(arr), 'Index recursion');

/*
🧠 Что ты получишь:
────────────────────────────
- [Slice recursion] Used heap: ~X MB  ← зависит от длины массива и нагрузки
- [Index recursion] Used heap: ~0 MB  ← почти не расходует память

📌 Почему без GC важен?
- Значения heapUsed показывают "грязную" картину: что осталось в памяти
- В реальных условиях GC вызывается не сразу, и мусор может оставаться
- Это хорошо отражает поведение кода **в продакшене**, где нет ручного GC

📌 Почему slice "жрёт" память:
- На каждом шаге создаётся новый массив → временные объекты
- Эти объекты копятся в памяти до тех пор, пока GC их не уберёт
- Если массив большой — легко получить повышение heapTotal и даже GC

📌 Почему index — лучше:
- Нет новых массивов, только увеличение индекса
- Один и тот же массив используется весь цикл
- Рекурсия безопасна (до разумной глубины) и память почти не тратится
*/
