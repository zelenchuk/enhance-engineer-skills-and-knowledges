
// ❌ Простая очередь на массиве с shift (медленно при большом объёме)
class SlowQueue {
  constructor() {
    this.queue = [];
  }

  enqueue(item) {
    this.queue.push(item); // O(1)
  }

  dequeue() {
    return this.queue.shift(); // ❌ O(n) — каждый раз сдвигает массив
  }
}

// ✅ Быстрая очередь на двух стеках (средняя сложность операций — O(1))
class Queue {
  constructor() {
    // 🔹 Стек для новых поступающих элементов
    // Элементы добавляются сюда через enqueue()
    this.inStack = [];

    // 🔹 Стек для удаления и чтения элементов
    // Когда он пуст, мы перекладываем в него всё из inStack
    this.outStack = [];
  }

  // 📥 Приём элемента в очередь
  enqueue(item) {
    // 🔹 Приём новых данных всегда идёт в конец inStack — это O(1)
    this.inStack.push(item); // ✅ Приём без сдвига массива
  }

  // 📤 Извлечение элемента из начала очереди
  dequeue() {
    // 🧠 Приём №1: отложенное копирование — перекладываем только при необходимости
    // Это **ленивый подход (lazy transfer)**, который делает перенос только тогда,
    // когда outStack пуст — т.е. максимально редко
    if (this.outStack.length === 0) {
      // 🧠 Приём №2: реверс через стек — быстро меняем порядок без сдвигов
      // Вместо того чтобы сдвигать массив (как shift()),
      // мы перекладываем элементы в обратном порядке в другой стек
      while (this.inStack.length > 0) {
        this.outStack.push(this.inStack.pop()); // ✅ Обратный перенос: O(1) на каждый шаг
      }
    }

    // 🧠 Приём №3: амортизированная сложность
    // Даже если происходит перенос — он делается "раз в N вызовов",
    // и в среднем каждый элемент обрабатывается всего один раз
    return this.outStack.pop(); // ✅ Быстрое удаление с конца — O(1)
  }
}


// ⚙️ Бенчмарк-функция
function benchmark(queueClass, label) {
  const q = new queueClass();

  const start = Date.now();

  // Эмуляция приёма 1000000 логов
  for (let i = 0; i < 1000000; i++) {
    q.enqueue(`log_${i}`);
  }

  // Эмуляция обработки этих логов
  for (let i = 0; i < 1000000; i++) {
    q.dequeue();
  }

  const end = Date.now();
  console.log(`${label}: ${end - start}ms`);
}

// 🧪 Запуск
console.log("🚀 Бенчмарк очередей на 1,000,000 элементов:");
benchmark(SlowQueue, "❌ Массив с shift()");
benchmark(Queue,      "✅ Очередь на двух стеках");
